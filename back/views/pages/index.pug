extends ../template

block content

    +b.DIV.main

        +e.H1.title.title Main page

        +e.A.link.link(href='/about') about
        +e.A.link.link(href='/contacts') contacts

        +test()
